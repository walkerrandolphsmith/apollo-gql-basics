// import cookieParser from 'cookie-parser';
// import cookieSession from 'cookie-session';
// import { buildPassort } from 'src/www/buildPassport';

// export const applyPassport = app => {
//   const passport = buildPassort();
//   app.use(cookieParser());
//   app.use(
//     cookieSession({
//       secret: 'secret',
//       cookie: { maxAge: 3600 },
//     })
//   );
//   app.use(passport.initialize());
//   app.use(passport.session());
// };
